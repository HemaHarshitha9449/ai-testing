# Calculator

A simple, elegant web-based calculator built with HTML, CSS, and JavaScript.

## Features

- Basic arithmetic operations: addition (+), subtraction (-), multiplication (×), division (÷)
- Clean and modern user interface
- Keyboard support for faster input
- Delete and clear functions
- Decimal number support
- Division by zero protection

## How to Use

### Running the Calculator

1. Open `index.html` in any modern web browser
2. The calculator will be ready to use immediately

### Using Mouse/Touch

- Click number buttons (0-9) to input numbers
- Click operator buttons (+, -, ×, ÷) to select operations
- Click `=` to calculate the result
- Click `C` to clear the display
- Click `DEL` to delete the last character

### Using Keyboard

- **Numbers**: Press 0-9 keys
- **Decimal**: Press `.` key
- **Operators**: Press `+`, `-`, `*`, or `/` keys
- **Calculate**: Press `Enter` or `=` key
- **Clear**: Press `Escape` key
- **Delete**: Press `Backspace` key

## Project Structure

```
ai-testing/
├── index.html      # Main HTML structure
├── style.css       # Styling and layout
├── script.js       # Calculator functionality
└── README.md       # Documentation
```

## Browser Compatibility

Works on all modern browsers including:
- Chrome
- Firefox
- Safari
- Edge

## License

Free to use and modify.
